\name{percent}
\alias{percent}
\title{The function to format data into percentage format}
\usage{
percent(x, digits = 2, format = "f", ...)
}
\arguments{
  \item{x}{is numeric}
}
\description{
The function to format data into percentage format
}
\examples{
data <- percent(x)
}

